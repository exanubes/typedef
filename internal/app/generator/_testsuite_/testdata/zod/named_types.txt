import { z } from "zod";

const UserSchema = z.object({
  id: z.number(),
  createdAt: z.date(),
  name: z.string(),
});
type User = z.infer<typeof UserSchema>;

const RootSchema = z.object({
  id: z.uuid(),
  author: UserSchema,
  cool: z.boolean(),
  created_at: z.date(),
  float: z.number(),
  mixed: z.array(z.union([UserSchema, z.boolean(), z.date(), z.number(), z.string()])),
  numbers: z.array(z.number()),
  title: z.string(),
  user: UserSchema,
});
type Root = z.infer<typeof RootSchema>;

